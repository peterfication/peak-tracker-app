# eslint-disable-next-line @graphql-eslint/naming-convention
query GetPeaks {
  peaks {
    edges {
      node {
        ...GetPeaksPeak
      }
    }
  }
}

fragment GetPeaksPeak on Peak {
  id
  name
  slug
}
