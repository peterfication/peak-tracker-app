import { LoginLoadingScreen } from '@app/screens';

const LoginLoadingScreenMeta = {
  title: 'Login Loading Screen',
  component: LoginLoadingScreen,
  argTypes: {},
  args: {},
};

export default LoginLoadingScreenMeta;

export const Basic = {};
